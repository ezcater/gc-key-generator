This is a hash generator for sharing secrets between 2 application

To use, simple import this library and use it as a function

```
const hasher = require('gc-key-generator');
const newHash = hasher();
```

